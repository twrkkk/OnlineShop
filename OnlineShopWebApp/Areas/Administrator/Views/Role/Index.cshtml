@using Microsoft.AspNetCore.Identity;
@model List<RoleViewModel>

<div class="row">
    @await Html.PartialAsync("LeftAdminMenu")


    <div class="col-9">
        <h3 class="text-center">Список ролей</h3>

        @if (Model == null || Model.Count == 0)
        {
            <h3 class="text-center"> Роли отсутствуют</h3>
        }
        else
        {
            <table id="orderList" class="table table-condensed table-responsive">
                <thead>
                    <tr>
                        <th style="width:30%">Роль</th>
                    </tr>
                </thead>
                @foreach (var role in Model)
                {
                    @await Html.PartialAsync("ModalDeleteRole", role)
                    <tbody>
                        <tr>
                            <td data-th="Роль">@role.Name</td>

                            <td class="col-md-2 text-left mt-sm-2">
                                <a style="color:black" data-toggle="modal" data-target="#deleteRole@(role.Name)">
                                    <h6>Удалить</h6>
                                </a>
                            </td>

                        </tr>
                    </tbody>
                }
            </table>
        }
        <button class="col-md-2 text-left mt-sm-2">
            <a style="color:black" asp-area="Administrator" asp-controller="Role" asp-action="Add">
                <h6>Создать роль</h6>
            </a>
        </button>
    </div>
</div>
