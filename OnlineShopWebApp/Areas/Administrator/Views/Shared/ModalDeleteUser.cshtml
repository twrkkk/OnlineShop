@model UserViewModel

@if (Model != null)
{
        <div class="modal fade" id="deleteUser@(Model.Id)" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Удаление пользователя</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    Вы уверены, что хотите удалить пользователя (@Model.RegistrationInfo.Email) навсегда?
                </div>
                <div class="modal-footer">
                    <a type="button" class="btn btn-primary" asp-area="Administrator" asp-controller="User" asp-action="Delete" asp-route-userEmail="@Model.RegistrationInfo.Email">Удалить</a>
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Отмена</button>
                </div>
            </div>
        </div>
    </div>
}